version: 1.1.0
name: app 
description: Foundries.io app example

---

version: '3.3'

services:
  nginx:
    image: hub.foundries.io/munoz0raul/app:latest
    restart: always
    ports:
      - 80:${PORT}

---

PORT: 80
