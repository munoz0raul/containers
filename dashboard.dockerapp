version: 1.1.0
name: dasahboard
description: Foundries.io dashboard example

---

version: '3.3'

services:
  nginx:
    image: hub.foundries.io/munoz0raul/dashboard:latest
    restart: always
    ports:
      - 80:${PORT}

---

PORT: 80
