version: 0.1.0
name: webserver 
description: Foundries.io webserver example

---

version: '3.2'

services:
  nginx:
    image: hub.foundries.io/munoz0raul/webserver:latest
    restart: always
    ports:
      - 80:${PORT}

---

PORT: 80
